apiVersion: v1
kind: Service
metadata:
  name: spotiplay
  namespace: spotiplay
spec:
  selector:
    app: spotiplay
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
